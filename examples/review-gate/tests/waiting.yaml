schema: agent-graph.test.v1
name: review requires the user without authority
graph: review
entry: default
state:
  outcomes:
    review/prepare: completed
expect:
  statusCode: waiting-user
  primaryNode: approve
  availability: requires-user
  gateResolution: user
  inspectionHandler: review.inspect-result
  resolutionHandler: review.apply-decision
  resolutionInputSchema: schema.apply-review.input
