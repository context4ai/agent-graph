---
name: produce-artifact
description: Produce the requested artifact and return its observable digest.
---

Create the artifact requested by the host. Report its location and digest so the host can observe them as facts.
